#include <stdint.h>

uint32_t setup_motors(void)
{
    /**
     * We don't have to setup anything to work with our ESC/MOTOR couple
     * but if you have something to setup, you can put it here
     */
    return (0);
}
